# Earlier GP UI prototype

This is the initial, read-only reconstruction used during simulator research. The active simulator copy is in `sim-app/`, and the patient app is in `web/`.

The source remains here because the research notes reference its components. Run it explicitly with `npm run dev:prototype` from the repository root. Normal `npm run dev` starts Kindred.
